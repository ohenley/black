project black is
   for Library_Name use "black";
   for Library_Version use "0.1.0";

   for Create_Missing_Dirs use "True";

   for Source_Dirs use ("src");
   for Object_Dir use "build/obj";
   for Library_Dir use "build/lib";
   for Library_Kind use "static";
end black;
